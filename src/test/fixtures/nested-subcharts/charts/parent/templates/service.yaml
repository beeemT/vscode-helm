apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-parent
  labels:
    app: parent
    environment: {{ .Values.global.environment }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: parent
